# blogin.dev

The Blogin documentation site, itself a Blogin instance (dogfooded). The source
tree lives at the repository root: `content/` holds the Markdown guide pages,
`layouts/` the HAML templates, `static/` the assets, and `blogin.json` the site
configuration.

## Building

Render the site with `blogin build` from the repository root.
