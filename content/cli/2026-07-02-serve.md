---
title: blogin serve
date: 2026-07-02
order: 4
toc: true
tags: [cli, builds]
description: Preview the site locally with rebuilds on change.
---
Build the site and serve it locally, rebuilding when content, layouts, static
assets, data files, or the configuration change. A successful rebuild live-reloads
the open page in the browser.

```
blogin serve [--src=<dir>] [--port=<n>]
```

## Options

`--src=<dir>`
: the content directory. It defaults to `content`.
`--port=<n>`
: the port to listen on. It defaults to `3000`.
`--verbose`
: prints extra detail.

## Example

```
blogin serve --port=4000
```
